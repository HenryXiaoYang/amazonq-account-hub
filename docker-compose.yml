version: '3.8'

services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - PASSKEY=${PASSKEY}
    volumes:
      - ./accounts.db:/app/accounts.db
    restart: unless-stopped
